import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerLogout: {
    flex: 1,
    alignItems: "flex-end",
    flexDirection: "row",
  },
  textLogout: {
    color: "#B00020",
  },
  textLogoutContainer: {
    width: "100%",
  },
});

export default styles;
